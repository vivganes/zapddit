name: GitHub Pages

on:
  push:
    branches:
    - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '16'

    - name: Install dependencies
      run: npm install


    - name: Build
      run: npm run build:prod && echo 'zapddit.com' > ./dist/zappedit/CNAME && cp ./dist/zappedit/index.html ./dist/zappedit/404.html

    - name: Deploy
      if: success()
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.ZAPPEDIT_TOKEN }}
        publish_dir: dist/zappedit
        enable_jekyll: true